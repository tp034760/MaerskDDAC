@model IEnumerable<Maersk_DDAC.Models.Ship>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShipName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShipBay)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShipName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShipBay)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ShipID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ShipID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ShipID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
